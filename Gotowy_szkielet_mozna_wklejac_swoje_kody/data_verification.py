# Marta


# Poniżej wpisałam cokolwiek, żeby tylko moduł main jakoś chwilowo działał
# Wszystkie funkcje: input: string z odpowiedznimi danymi, output: Bool
def phone_verification(
    data,
):  # nazwa argumentu może się zmienić, nie ma to znaczenia na zewnątrz funkcji
    # print("Data is " + data)
    # result = input("Input T if True or F if False").lower()
    print("     Tu wskoczy kod Marty (phone verification), na razie jest prowizorka\n")
    result = "t"
    if result == "t":
        return True
    else:
        return False


def email_verification(
    data,
):  # nazwa argumentu może się zmienić, nie ma to znaczenia na zewnątrz funkcji
    # print("Data is " + data)
    # result = input("Input T if True or F if False").lower()
    print("     Tu wskoczy kod Marty (email verification), na razie jest prowizorka\n")
    result = "t"
    if result == "t":
        return True
    else:
        return False


def birthday_verification(
    data,
):  # nazwa argumentu może się zmienić, nie ma to znaczenia na zewnątrz funkcji
    # print("Data is " + data)
    # result = input("Input T if True or F if False").lower()
    print(
        "     Tu wskoczy kod Marty (birthday verification), na razie jest prowizorka\n"
    )
    result = "t"
    if result == "t":
        return True
    else:
        return False
